import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app-pedidos-ionic-capacitor',
  webDir: 'www'
};

export default config;
